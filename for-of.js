// The 'for/off' loop is similar to foreach/in loop in other languages.
// You can loop through each element of a string, list, etc. and manipulate it.

const str = "word";

    for (let char of str) {
        console.log(char);
    }